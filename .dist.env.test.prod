# POSTGRES
POSTGRES_USER=your_postgres_user
POSTGRES_PASSWORD=your_postgres_password
POSTGRES_DB=url_template_db
POSTGRES_HOST=test_db
POSTGRES_PORT=5432

POSTGRES_DB_TEST_NAME=url_test_db

DB_URI_TEMPLATE="postgresql+psycopg://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_HOST}:${POSTGRES_PORT}/${POSTGRES_DB}"
DB_URI_TEST="postgresql+psycopg://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_HOST}:${POSTGRES_PORT}/${POSTGRES_DB_TEST_NAME}"
DB_URI="postgresql+psycopg://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_HOST}:${POSTGRES_PORT}/${POSTGRES_DB_TEST_NAME}"

# URL
HOST=localhost:8000
PROTOCOL=http

# JWT
SECRET_KEY=your_secret_key
EXPIRE=5
ALGORITHM=HS256
