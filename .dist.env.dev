# POSTGRES
POSTGRES_USER=your_postgres_user
POSTGRES_PASSWORD=your_postgres_password
POSTGRES_DB=your_database_name
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
DB_URI="postgresql+psycopg://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_HOST}:${POSTGRES_PORT}/${POSTGRES_DB}"

# URL
HOST=localhost:8000
PROTOCOL=http

# JWT
SECRET_KEY=your_secret_key
EXPIRE=5
ALGORITHM=HS256
